#include<stdio.h>
// This programme is to calculate the area of a square when the side of the square is given. 
int main() {
float side;

printf("Enter the side of square in centimeters  ");
scanf("%f", &side);

printf("The area of the square is %f", side * side);

return 0;
}
// same programme can also be used to square a number.
